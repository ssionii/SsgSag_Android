<shape xmlns:android="http://schemas.android.com/apk/res/android"
        android:shape="rectangle">
    <solid android:color="@color/selectedTabColor" />
    <stroke
            android:width="1dp"
            android:color="@color/selectedTabColor" />
    <corners android:radius="4dp" />
</shape>